
const RepoConfigService = {

    // create new
    createNew(id: number){

    },
    // delete by id
        // if last, create one new
    deleteById(id: number){

    }
}

export default RepoConfigService;
