import React from "react";

const Repository = ({conf: ReqConfigSingle}) => {

    const header = (
        <>

        </>
    );

    const footer = (
        <>

        </>
    );

    return (
        <React.Fragment>
            {header}

            {footer}
        </React.Fragment>
    )
}
export default Repository;
